export interface Location{
    id: string; // Use 'string' if IDs are not strictly numeric
    
    name: string;
    climate: string;
    tag : string;
    flighttype: string;
    imgurl : string;
    visited: boolean;

 }